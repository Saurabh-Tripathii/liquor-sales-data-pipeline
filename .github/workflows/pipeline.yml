name: End-to-End Data Pipeline

on:
  workflow_dispatch:
    inputs:
      raw_path:
        required: true
      clean_path:
        required: true

jobs:
  pipeline:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init
        working-directory: terraform

      - name: Terraform Apply
        run: |
          terraform apply -auto-approve \
          -var="raw_bucket_name=$(echo '${{ github.event.inputs.raw_path }}' | cut -d/ -f3)" \
          -var="clean_bucket_name=$(echo '${{ github.event.inputs.clean_path }}' | cut -d/ -f3)"
        working-directory: terraform

      - name: Run Glue Job
        run: |
          aws glue start-job-run \
            --job-name liquor-sales-cleaning-job \
            --arguments "{
              \"--RAW_S3_PATH\": \"${{ github.event.inputs.raw_path }}\",
              \"--CLEAN_S3_PATH\": \"${{ github.event.inputs.clean_path }}\"
            }"

      - name: Wait
        run: sleep 60

      - name: Run Glue Crawler
        run: aws glue start-crawler --name liquor-cleaned-data-crawler
